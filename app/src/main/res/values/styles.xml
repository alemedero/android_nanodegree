<resources>

    <!-- extend one of the Theme.AppCompat themes -->
    <style name="Theme.CustomTheme" parent="Theme.AppCompat.Light">
        <!-- customize the color palette -->
        <item name="colorPrimary">@color/material_blue_700</item>
        <item name="colorPrimaryDark">@color/material_blue_800</item>
        <item name="colorAccent">@color/material_blue_50</item>
        <item name="android:textColorPrimary">@android:color/white</item>
        <item name="colorButtonNormal">@color/button_background</item>
    </style>

</resources>
